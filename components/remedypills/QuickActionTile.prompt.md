RemedyPills home-screen quick-action button: pastel rounded-square icon over a small label, used in a 4-column grid (Prescription/Refill/Consult/Health).

```jsx
<QuickActionTile icon={<Pill />} label="Prescription" tone="teal" />
```
