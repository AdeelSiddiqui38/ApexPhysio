Small pill-shaped status/label chip, modeled on RemedyPills' `StatusPill` (Active/Processing/Ready for Pickup/Completed).

```jsx
<Badge tone="ready">Ready for Pickup</Badge>
```

Tones map 1:1 to prescription statuses; `neutral`/`primary` are general-purpose.
